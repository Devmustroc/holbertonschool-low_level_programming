#include <stdio.h>
/**
* main - Entry point
*
* Return: Always 0 (success)
*/
int main(void)
{
	int Number;

	for (Number = 0 ; Number <= 9 ; Number++)
		printf("%d", Number);
	puts("");
	return (0);
}
