 #include "main.h"
/**
 * jack_bauer - define is a character is alphabetic
 *
 * Return: - (void)
 */
void jack_bauer(void)
{
	int c, n;

	for (c = 0 ; c <= 23; c++)
	{
		for (n = 0 ; n <= 59; m++)
		{
			_putchar((c / 10) + '0');
			_putchar((c % 10) + '0');
			_putchar(':');
			_putchar((n / 10) + '0');
			_putchar((n % 10) + '0');
			_putchar('\n');
		}
	}
}
